# Copy to .env and fill in your key for LLM stages (classification, drafting, judging).
# Pipeline runs in demo mode without a key — keyword baselines + template replies only.
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini
OPENAI_JUDGE_MODEL=gpt-4o-mini
